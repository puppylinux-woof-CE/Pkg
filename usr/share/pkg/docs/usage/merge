USAGE="Usage: ${bold}pkg merge PKGNAME1,PKGNAME2,..${endcolour}

Merge multiple packages into a single package. The newly
created package file will a .PET.

PKGNAME1,PKGNAME2 (etc) must be ${bold}comma separated (no spaces)${endcolour}!
"
